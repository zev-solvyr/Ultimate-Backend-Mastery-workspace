import { readFileSync, writeFileSync } from "fs";
import { resolve } from "path";

const root = resolve(import.meta.dirname, "..");
const jvm = {
  "Java Architecture: JDK, JRE and JVM": [4, "The JDK is the development kit containing tools such as javac; the JRE is the runtime libraries and JVM needed to run Java; the JVM executes bytecode. Java source is compiled once to bytecode and each platform's JVM interprets or JIT-compiles it. This separation is why Java is portable at the bytecode level, not because native machine code is shared.", "String source = \"class HealthCheck { boolean ready() { return true; } }\";\n// javac compiles source to HealthCheck.class bytecode; the JVM loads and executes it."],
  "Compilation, Bytecode and Platform Independence": [4, "javac performs compile-time checks and emits platform-neutral bytecode into .class files. The JVM verifies, loads, interprets, and may JIT-compile hot bytecode into native instructions. Native libraries and JVM implementations remain platform-specific, so platform independence has boundaries.", "// Compile: javac -d out HealthCheck.java\n// Run on any compatible JVM: java -cp out HealthCheck\npublic final class HealthCheck { boolean ready() { return true; } }"],
  "Class Loading and Class Loaders": [4, "Class loading finds bytecode, linking verifies and prepares it, and initialization runs static initializers on first active use. The bootstrap, platform, and application loaders delegate parent-first to avoid loading two incompatible definitions of core classes. In containers, class-loader isolation can cause ClassCastException when the same class name is loaded by different loaders.", "ClassLoader loader = PaymentService.class.getClassLoader();\nClass<?> type = Class.forName(\"com.example.PaymentService\", true, loader);\nObject service = type.getDeclaredConstructor().newInstance();"],
  "Runtime Data Areas": [4, "Each thread has its own program counter, JVM stack, and native-method stack; the heap and metaspace are shared. Stack frames hold local variables and method state, while heap objects are reached through references. This distinction explains why local variables are thread-confined but mutable shared objects need synchronization.", "static void process(Order order) {\n    int retry = 0;           // local value in this call's stack frame\n    order.status = \"PAID\"; // object lives on the shared heap\n}\nstatic final class Order { String status; }"],
  "Heap, Stack and Metaspace": [5, "Objects and arrays are allocated on the heap, while each method invocation gets a stack frame containing locals and operand state. Metaspace stores class metadata and grows in native memory rather than the old fixed PermGen area. Heap pressure causes garbage collection or OutOfMemoryError; unbounded recursion consumes stack frames and causes StackOverflowError.", "static final Map<String, byte[]> CACHE = new ConcurrentHashMap<>();\nstatic void cache(String id, byte[] payload) { CACHE.put(id, payload); }\n// An unbounded static cache retains heap objects and can cause OutOfMemoryError."],
  "JIT Compilation": [3, "The JVM initially interprets bytecode and uses profiling information to JIT-compile hot methods into optimized native code. Optimizations can be invalidated and recompiled when runtime assumptions change. Benchmarking must include warm-up because cold interpreted execution is not representative of steady state.", "static long sum(long[] values) {\n    long total = 0;\n    for (long value : values) total += value;\n    return total; // a hot loop is a typical JIT optimization candidate\n}"],
  "Garbage Collection": [5, "Garbage collection reclaims heap objects that are no longer strongly reachable from GC roots. Modern collectors are generational because most objects die young; collection behavior and pause goals depend on the selected collector. GC does not prevent leaks when a static collection, listener, cache, or ThreadLocal still retains a reference.", "static final List<Runnable> listeners = new ArrayList<>();\nstatic void register(Runnable listener) { listeners.add(listener); }\n// Registered listeners remain reachable until removed; GC cannot reclaim them."],
  "Java Memory Model and happens-before": [5, "The Java Memory Model defines visibility and ordering between threads, not merely CPU behavior. A happens-before edge is created by monitor unlock-to-lock, volatile write-to-read, thread start/join, and executor task submission/completion rules. Without such an edge, one thread may observe stale values even when another thread has assigned them.", "private volatile boolean shuttingDown;\nvoid stop() { shuttingDown = true; }\nboolean shouldAcceptRequests() { return !shuttingDown; }\n// The volatile write is visible to later volatile reads."],
  "GC Roots and Reference Types": [4, "GC roots include live thread stacks, static fields, JNI references, and active JVM structures. An object is retained if reachable from a root, directly or through another object. Weak references allow cache-like entries to be cleared under collection pressure, but they are not a substitute for explicit cache bounds or eviction.", "Map<String, WeakReference<byte[]>> previews = new ConcurrentHashMap<>();\npreviews.put(fileId, new WeakReference<>(thumbnail));\nbyte[] cached = Optional.ofNullable(previews.get(fileId)).map(WeakReference::get).orElse(null);"],
  "Memory Leaks and OutOfMemoryError": [5, "A Java memory leak is unintended retention: memory is managed automatically, but reachable objects cannot be reclaimed. Common causes are static collections, unbounded caches, listeners, executor queues, and ThreadLocal values in pooled threads. Diagnose with heap dumps and retained-size paths rather than assuming GC is broken.", "ThreadLocal<byte[]> requestBuffer = new ThreadLocal<>();\ntry { requestBuffer.set(new byte[1024 * 1024]); process(); }\nfinally { requestBuffer.remove(); } // essential with pooled threads"],
  "StackOverflowError": [3, "StackOverflowError occurs when call depth exhausts a thread's stack, commonly from missing recursion termination or cyclic calls. Increasing stack size can mask a bug but does not make unbounded request-driven recursion safe. Prefer iterative traversal when input depth is uncontrolled.", "static int depth(Node node) {\n    int depth = 0;\n    for (Node current = node; current != null; current = current.next) depth++;\n    return depth; // iterative traversal avoids recursive stack growth\n}\nstatic final class Node { Node next; }"],
};

const oop = {
  "Classes, Objects and Constructors": [4, "A class defines state and behavior; an object is a runtime instance with its own state. Constructors establish valid invariants so callers cannot create partially initialized domain objects. Keep construction focused on mandatory state and move complex workflows to services or factories.", "record Money(BigDecimal amount, Currency currency) {\n    Money { if (amount.signum() < 0) throw new IllegalArgumentException(\"negative amount\"); }\n}\nMoney total = new Money(new BigDecimal(\"42.00\"), Currency.getInstance(\"USD\"));"],
  "Encapsulation": [5, "Encapsulation keeps an object's invariants behind behavior rather than exposing mutable fields. A caller should request a state transition such as reserve() instead of directly changing available quantity. This localizes validation and prevents invalid state from spreading through a service.", "final class Inventory {\n  private int available;\n  boolean reserve(int quantity) { if (quantity <= 0 || quantity > available) return false; available -= quantity; return true; }\n}"],
  "Inheritance": [4, "Inheritance models a genuine is-a relationship and lets a subtype honor its base type contract. It is appropriate for stable polymorphic hierarchies, but fragile when subclasses need to change inherited behavior or expose base implementation details. Prefer composition for reusable capabilities and changing business rules.", "abstract class Payment { abstract Receipt authorize(Money amount); }\nfinal class CardPayment extends Payment { Receipt authorize(Money amount) { return Receipt.approved(); } }"],
  "Polymorphism": [5, "Polymorphism lets code depend on an abstraction while runtime dispatch selects the implementation. Overloading is compile-time selection by parameter types; overriding is runtime dispatch on an object's actual type. Backend services use this to substitute payment, notification, storage, or policy implementations without changing the caller.", "interface PaymentProcessor { Receipt charge(Order order); }\nfinal class CheckoutService {\n  private final PaymentProcessor processor;\n  CheckoutService(PaymentProcessor processor) { this.processor = processor; }\n  Receipt checkout(Order order) { return processor.charge(order); }\n}"],
  "Abstraction and Interfaces": [5, "Abstraction exposes the behavior a caller needs while hiding implementation details. Interfaces define a contract and support multiple implementations; abstract classes are useful when closely related implementations share protected state or template behavior. Choose an interface for a service boundary and an abstract class only when shared implementation is intentional.", "interface NotificationSender { void send(Notification notification); }\nfinal class EmailSender implements NotificationSender { public void send(Notification n) { /* send via SMTP client */ } }"],
  "Composition vs Inheritance": [5, "Composition gives an object collaborators and delegates work to them; inheritance reuses a base implementation through an is-a relationship. Composition is usually safer for backend services because dependencies can be replaced, tested, and configured independently. Use inheritance only when substitutability is preserved.", "final class OrderService {\n  private final TaxCalculator taxes;\n  OrderService(TaxCalculator taxes) { this.taxes = taxes; }\n  Money total(Order order) { return order.subtotal().add(taxes.calculate(order)); }\n}"],
  "Access Modifiers": [3, "Access modifiers enforce API boundaries: public exposes a contract, private protects implementation, protected exposes inheritance hooks, and package-private supports collaboration inside a package. Prefer the narrowest visibility that supports the intended use. Public mutable fields make invariant enforcement difficult.", "public final class Account {\n  private BigDecimal balance;\n  void applyInterest(BigDecimal rate) { balance = balance.multiply(BigDecimal.ONE.add(rate)); } // package-private\n}"],
  "static, final, finally and finalize": [4, "static belongs to the class, final prevents reassignment, overriding, or subclassing depending on context, and finally runs cleanup code after try/catch except in abnormal JVM termination. finalize is deprecated and unreliable for resource cleanup; use AutoCloseable and try-with-resources instead.", "try (Connection connection = dataSource.getConnection()) {\n    return connection.isValid(2);\n} // close executes deterministically; do not rely on finalize()"],
  "equals and hashCode": [5, "equals defines logical equality and must be reflexive, symmetric, transitive, consistent, and false for null. Equal objects must return the same hashCode; otherwise HashMap and HashSet can place equal keys in different buckets and fail lookup. Use immutable fields that identify the value, and never mutate fields used by hashCode while the object is a map key.", "record CustomerId(UUID value) {}\nMap<CustomerId, String> names = new HashMap<>();\nCustomerId id = new CustomerId(UUID.randomUUID());\nnames.put(id, \"Ada\");\nassert names.get(new CustomerId(id.value())).equals(\"Ada\"); // record supplies consistent equals/hashCode"],
  "toString, clone and getClass": [3, "toString should provide useful diagnostic state without leaking secrets. getClass returns the runtime class and is often used for strict equality checks; instanceof allows subtype equality but requires careful symmetry reasoning. clone is a legacy shallow-copy mechanism with awkward construction rules; prefer copy constructors, factories, or records.", "record PaymentAttempt(UUID id, String status) {\n  @Override public String toString() { return \"PaymentAttempt[id=\" + id + \", status=\" + status + \"]\"; }\n}\nClass<?> runtimeType = new PaymentAttempt(UUID.randomUUID(), \"APPROVED\").getClass();"],
  "Immutable Objects": [5, "An immutable object cannot change observable state after construction. It is naturally thread-safe, stable as a map key, and easier to reason about in concurrent services. Defensively copy mutable inputs and do not expose mutable internals.", "final class ApiKey {\n  private final byte[] value;\n  ApiKey(byte[] value) { this.value = value.clone(); }\n  byte[] value() { return value.clone(); }\n}"],
  "Enum": [3, "An enum is a fixed set of singleton instances with type safety and can contain fields and behavior. Use it for closed domain states such as order status; avoid persisting ordinal values because reordering constants changes their meaning. Persist a stable string code when the database contract matters.", "enum OrderStatus {\n  CREATED(false), PAID(false), SHIPPED(false), CANCELLED(true);\n  final boolean terminal; OrderStatus(boolean terminal) { this.terminal = terminal; }\n}"],
};

const records = { ...jvm, ...oop };
const file = resolve(root, "src/data/roadmap.json"); const roadmap = JSON.parse(readFileSync(file, "utf8"));
const topics = roadmap.levels.find((level) => level.level === 1).topics.filter((topic) => records[topic.title]);
for (const topic of topics) { const [priority, definition, code] = records[topic.title]; topic.interviewPriority = priority; topic.interviewDefinition = definition; topic.interviewQuestions = [[`What is the key interview distinction for ${topic.title}?`, definition.split(". ")[0]], [`What production risk or trade-off matters for ${topic.title}?`, definition.split(". ").slice(-2).join(". ")]].map(([question, answer]) => ({ question, answer })); topic.codeExamples = [{ title: `${topic.title} — interview reference`, language: "java", code }]; topic.personalNotes = ""; }
if (topics.length !== 23) throw new Error(`Expected 23 JVM/OOP topics, found ${topics.length}`);
writeFileSync(file, `${JSON.stringify(roadmap, null, 2)}\n`); console.log(`Updated ${topics.length} Level 1 JVM/OOP topics.`);
