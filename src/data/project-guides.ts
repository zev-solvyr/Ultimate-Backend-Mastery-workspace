import type { ProjectGuide } from "@/types";
import { defaultProjectBlueprints } from "./project-blueprints-seed";

export const projectGuides: Record<string, ProjectGuide> = defaultProjectBlueprints;
