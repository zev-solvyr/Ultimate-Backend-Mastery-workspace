import type { ProjectDomainModel } from "@/types";
const entity = (name: string, purpose: string, owningService: string, persistence: string, importantFields: string[], relationships: string[]) => ({ name, purpose, owningService, persistence, importantFields, relationships });
export const commercexDomain: ProjectDomainModel = { entities: [entity("Customer", "Identity and customer profile used to authorize commerce actions.", "Auth Service", "PostgreSQL", ["id", "email", "displayName", "status", "createdAt"], ["Referenced by Cart and Order through customerId."]), entity("Product", "Sellable catalog item with a stable SKU and current price.", "Catalog Service", "PostgreSQL; optional document projection", ["id", "sku", "name", "description", "price", "currency", "categoryId", "status"], ["Belongs to Category; referenced by CartItem and OrderItem by productId."]), entity("Category", "Catalog grouping for navigation and product discovery.", "Catalog Service", "PostgreSQL", ["id", "name", "parentCategoryId", "status"], ["One Category has many Products within Catalog's database."]), entity("Cart", "Mutable customer shopping state before checkout.", "Cart Service", "Redis with durable backing when needed", ["id", "customerId", "updatedAt", "expiresAt"], ["Contains CartItems; product references are service-level IDs."]), entity("CartItem", "Selected product, quantity, and price snapshot in a cart.", "Cart Service", "Redis/document value", ["cartId", "productId", "quantity", "unitPrice"], ["Belongs to Cart; references Catalog Product without a cross-service foreign key."]), entity("Inventory", "Available and reserved stock for one product.", "Inventory Service", "PostgreSQL", ["productId", "availableQuantity", "reservedQuantity", "version"], ["Product reference is an ID from Catalog; has many reservations."]), entity("InventoryReservation", "Expiring allocation of stock for a checkout attempt.", "Inventory Service", "PostgreSQL", ["id", "orderId", "productId", "quantity", "status", "expiresAt"], ["References Order by ID; no cross-service database join."]), entity("Order", "Auditable lifecycle of a customer purchase.", "Order Service", "PostgreSQL", ["id", "customerId", "status", "totalAmount", "currency", "idempotencyKey", "createdAt"], ["Contains OrderItems; references Payment and Shipment by service-level IDs."]), entity("OrderItem", "Immutable product and price snapshot at purchase time.", "Order Service", "PostgreSQL", ["orderId", "productId", "sku", "quantity", "unitPrice"], ["Belongs to Order; retains product snapshot to avoid later catalog changes."]), entity("Payment", "Idempotent payment attempt and provider outcome.", "Payment Service", "PostgreSQL", ["id", "orderId", "amount", "status", "providerReference", "idempotencyKey"], ["References Order through orderId and publishes payment events."]), entity("Shipment", "Fulfillment and carrier tracking state.", "Fulfillment/Shipping Service", "PostgreSQL", ["id", "orderId", "carrier", "trackingNumber", "status", "shippedAt"], ["References Order through orderId; status updates are event-derived."]), entity("Notification", "Delivery attempt for customer-facing order updates.", "Notification Service", "PostgreSQL", ["id", "customerId", "channel", "template", "status", "eventId"], ["Created from Order, Payment, and Shipment events."])], relationships: [{ from: "Customer", to: "Order", type: "service-level reference", description: "Order stores customerId; Order Service does not foreign-key into Auth data." }, { from: "Customer", to: "Cart", type: "service-level reference", description: "Cart is keyed by customerId and independently expires." }, { from: "Category", to: "Product", type: "database relationship", description: "Catalog owns this relationship and enforces it locally." }, { from: "Cart", to: "CartItem", type: "owned aggregate", description: "Cart Service persists cart items with cart state." }, { from: "Order", to: "OrderItem", type: "owned aggregate", description: "Order Service stores immutable purchase snapshots." }, { from: "Order", to: "Payment / Shipment", type: "event-derived reference", description: "Independent services report state through events rather than joins." }, { from: "Inventory", to: "InventoryReservation", type: "database relationship", description: "Inventory Service uses reservations to protect available quantity." }], consistency: ["Each service is strongly consistent inside its own transaction; Inventory updates quantity and reservation together using versioned writes.", "Order, payment, and shipment statuses converge through events. The UI must represent pending states rather than assume immediate global consistency.", "Cross-service IDs are references, not foreign keys. Services consume events or call APIs instead of joining another service database.", "Payment idempotency and inventory reservations make retried checkout safe; compensation releases a reservation when payment cannot complete."] };
